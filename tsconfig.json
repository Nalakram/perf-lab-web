{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    },
    // ... keep all your existing options (target, jsx, module, etc.)
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]   // if you have this file
}